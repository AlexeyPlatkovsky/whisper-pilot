Read AGENTS.md before any work is started. Follow its instructions. Don't violate any of them.

## Debugging

Run the app with `RUST_LOG=info npm run tauri:dev` and capture stderr before drawing conclusions from behaviour alone. Transcription decodes are logged at debug level.
