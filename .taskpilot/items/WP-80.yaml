schema_version: 1
id: WP-80
title: 'llm.rs: drop RU/EN prompt branch, use loaded prompt files'
priority: normal
type: task
status: backlog
created_at: '2026-08-08T15:11:42Z'
updated_at: '2026-08-08T15:11:42Z'
parent_id: WP-78
description: build_prompt (MFU) and build_prettify_prompt in src-tauri/src/llm.rs currently each hardcode two full parallel prompt bodies, Russian and English, chosen by scanning the transcript for Cyrillic characters. Replace both with a single call path that takes the effective instruction text from prompts.rs (WP-79) as a parameter and splices it into the existing code-owned chat-template scaffolding (<|im_start|> markers) and, for MFU, the JSON output contract (summary/decisions/action_items/open_questions/participants) that parse_notes_json depends on. Each prompt's new compiled-in default (src-tauri/prompts/mfu.default.txt, src-tauri/prompts/prettify.default.txt) is authored in English and instructs the model to respond in the same language as the input transcript, rather than switching template bodies.
dod:
- build_prompt and build_prettify_prompt no longer branch on Cyrillic detection; each takes loaded instruction text and splices it into the unchanged code-owned scaffolding.
- src-tauri/prompts/mfu.default.txt and prettify.default.txt each contain one English-authored instruction set that tells the model to mirror the input transcript's language.
- 'Existing Rust unit tests that assert on the old RU/EN branch (build_prompt_uses_russian_for_cyrillic_transcript, build_prettify_prompt_uses_russian_for_cyrillic_transcript, and their English counterparts) are replaced with tests appropriate to the single-prompt behavior.'
- 'Manual verification: MFU and Prettify output quality is checked against real Russian, English, and ideally mixed-language transcripts after the branch removal, per CLAUDE.md''s RUST_LOG=info npm run tauri:dev debugging note.'
- cargo test, cargo clippy, and cargo fmt --check pass.
created_by: Aleksei_Platkovskii
