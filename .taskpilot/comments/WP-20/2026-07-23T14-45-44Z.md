---
schema_version: 1
created_at: '2026-07-23T14:45:44Z'
created_by: Aleksei_Platkovskii
---

Promoted backlog -> ready for route run WP-20:fix-bug:1.

DoR evidence: verify-readiness attempt 2 -> 'DoR gate: Ready' (all 7 criteria pass) against the record rewritten this run. Scope approval provenance: the user's instruction in this run removed the language-selection UI from the product entirely ('user shouldn't be able to choose the trancription language, it should be only autodetect'), which supersedes ADR-007's Russian-default-plus-opt-in-toggle decision and its explicitly rejected 'auto-detect as the only mode' alternative. The item was retitled from 'Language selection UI (Russian / auto-detect)' accordingly; it now delivers auto-detect-only transcription instead of a selector.

Root cause is confirmed and reproduced byte-for-byte outside the app (forced ru -> 11 identical 'Субтитры сделал DimaTorzok' segments, one per 30s window; auto -> DETECTED LANG: en and a correct transcript). Parent context WP-13 (feature) -> WP-11 (epic, in_progress).
