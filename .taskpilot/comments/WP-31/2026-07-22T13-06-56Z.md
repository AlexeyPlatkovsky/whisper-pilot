---
schema_version: 1
created_at: '2026-07-22T13:06:56Z'
created_by: Aleksei_Platkovskii
---

Implementation complete. TDD: 3 tests for assign_speaker_ids + 4 tests for apply_diarization_outcome (Red via unimplemented!() stubs, then Green) - the JoinError test uses a genuine tokio::task::spawn_blocking(|| panic!()).await, not a synthetic error. Full crate suite: 53 passed, 2 skipped, 0 failed (up from 46). clippy clean. cargo build clean. code-reviewer: first pass Needs revision (2 Major: untested degrade-orchestration logic: DoD/BDD gap; stale docs/architecture.md - both fixed; 1 Minor: double audio decode deviating from the item's own scope text - fixed to decode once, off the reactor via its own spawn_blocking). Second pass: Approved with minor notes (only an out-of-scope Info note on unrelated pre-existing doc text). Manual end-to-end verification (real downloaded models + real ~1-minute audio fixture) run twice - before and after the refactor - both times correctly producing 12 real Whisper segments all populated with speaker_id: Some(0) (single-speaker fixture). transcribe_file's happy path, asset-missing degrade, engine-error degrade, and task-panic degrade are all covered (3 by unit test, happy path by both unit test and real manual run). Docs updated: docs/architecture.md (Speaker Diarization section rewritten to describe the actual auto-run/single-decode/fail-open wiring).
