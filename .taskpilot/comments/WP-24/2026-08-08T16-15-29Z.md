---
schema_version: 1
created_at: '2026-08-08T16:15:29Z'
created_by: Aleksei_Platkovskii
---

DoD evidence: 283 Rust tests pass (cargo nextest, +3 new: export_file_type defaults to plain_text, persists across restart, rejects an invalid value and leaves the store unchanged), clippy -D warnings clean, cargo build clean, cargo fmt --check clean; 238 frontend tests pass (vitest, +14 new: export.ts unit tests for renderPlainText/renderMarkdown/renderForExport/exportFileExtension, ExportSection radio-group load/select/persist/revert-on-error tests, App-level Save/Copy-use-Markdown-when-selected tests), tsc --noEmit clean, eslint clean, prettier --check clean, vite production build succeeds. Implementation: settings.rs export_file_type (plain_text default, validated); src/export.ts renderForExport is the one function both Save and header Copy call so they cannot render differently; Markdown adds a # Transcript heading, bold speaker labels, [m:ss] timestamps, and a ## Notes section (one ### subsection per non-empty notes field) when notes exist; plain text is byte-identical to the pre-existing rendering. New Settings > Export screen (ExportSection.tsx) exposes the radio choice, following AppearanceSection's existing pattern. docs/architecture.md and docs/design.md updated. Native Tauri UI smoke not run in this session (no interactive display) — external/pre-existing limitation, not a found defect.
