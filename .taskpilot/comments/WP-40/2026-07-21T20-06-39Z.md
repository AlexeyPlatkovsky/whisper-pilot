---
schema_version: 1
created_at: '2026-07-21T20:06:39Z'
created_by: Aleksei_Platkovskii
---

Implemented: src-tauri/src/models.rs (+delete_model, +primary_asset_path -- path always built from static catalog literals, cannot escape the app models dir), src-tauri/src/transcribe.rs (load_model/model_path now take app_support_dir; override-vs-default decision extracted into pure resolve_model_path() for env-var-mutation-free testing; default repointed from the old VoicePilot-shared path to WP-39's download location -- explicit user-approved fix so 'downloaded via Settings' and 'what transcribe.rs loads' are the same fact), src-tauri/src/lib.rs (+delete_model command, AppState.model()/transcribe_file thread app_support_dir), src/ipc.ts (+deleteModel), src/AiModelsSection.tsx (+Delete button), src/App.tsx (+transcriptionModelReady gating Add-file, refreshed on Settings close). TDD: 7 Rust tests + 6 frontend tests, Red->Green. Validation: clippy/test/build/tsc/vitest all pass (23 Rust, 18 frontend). Manual UI verification: full live run -- deleted the real downloaded Whisper file via the UI, confirmed removal on disk; confirmed Add-file's disabled state was true at the OS accessibility level (not just CSS) after deletion; re-downloaded and re-verified SHA-256. Code review: two rounds -- first Approved with 2 Minor notes (missing adversarial test for a rejected listTaskModels() call; a duplicated hardcoded literal in the model-path fallback); both fixed. Fixing the first Minor note surfaced and fixed a real test-sequencing bug (AiModelsSection independently calls listTaskModels() when Settings opens, which a sibling test hadn't accounted for) plus added proper mock-reset test isolation. Final verdict: Approved. Documentation: fixed a stale docs/architecture.md claim ('own model management is deferred') that WP-39/WP-40 superseded. Explicit scope decisions recorded: diarization availability wiring deferred (no diarization feature exists in this codebase, WP-1's job); transcribe.rs model-path repointing was an explicit user-approved fix, not silent scope creep.
