---
schema_version: 1
created_at: '2026-07-26T11:29:34Z'
created_by: Aleksei_Platkovskii
---

Created by splitting WP-53 (user decision, 2026-07-26). Blocked by WP-53: the fallback needs a survivable child-process crash to react to, so it is not implementable until the isolation exists.

Carries design decisions 3, 4 and 5 of the approved Option D remediation, moved from WP-53, plus the accepted no-provenance consequence which only bites once a run can silently switch models.

Evidence this fallback is worth building rather than just failing open: the 2026-07-26 triage ran the exact pair. The 92.4s recording that reliably SIGBUSes titanet-large at threshold 0.9 completes cleanly under campplus at the same threshold (1 cluster). So on the one input known to break, the fallback returns a real answer instead of speaker-less segments.

Status backlog; needs DoR and promotion before implementation, same as WP-53.
