---
schema_version: 1
created_at: '2026-08-10T13:10:29Z'
created_by: Aleksei_Platkovskii
---

Completion evidence (WP-85:fix-bug:1, task-quality gate: pass).

Fix: src-tauri/src/commands/transcription.rs gained ensure_non_empty_transcript, called from decode_and_transcribe (Meeting-only path) immediately after decode - before persist and before the diarization pass is constructed. A zero-segment decode (empty-output signature of the intermittent ggml Metal fault, or a speechless file) now fails the run with AppError::Transcribe advising a retry, instead of persisting an empty, finished meeting with a bogus language. The shared decode (transcribe/transcribe_with_state) stays tolerant so silent Streaming windows are unaffected; cancel semantics (WP-19) unchanged - the guard is unreachable on the Cancelled path.

DoD evidence:
1-2. Unit tests ensure_non_empty_transcript_rejects_a_zero_segment_decode_with_retry_guidance / _passes_a_decode_with_segments - Green (red-first provenance: compile-fail E0425 pre-implementation).
3. Guard placement verified by code review (code-reviewer: Approved) - before diarization construction and persist; Streaming path untouched.
4. WP-19/WP-84 cancel tests all Green (295/295 cargo test).
5. Skill validate PASS; test-runner Pass 7/7 (cargo test 295, clippy 0 warnings, fmt, vitest 259/259, eslint, tsc, coverage lines 97.58%).
6. docs/architecture.md Transcription section updated (empty decode now fails the Meeting run); sdd-index-sync completed, no row changes.
