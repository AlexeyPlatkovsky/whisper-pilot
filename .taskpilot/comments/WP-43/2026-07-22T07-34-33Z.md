---
schema_version: 1
created_at: '2026-07-22T07:34:33Z'
created_by: Aleksei_Platkovskii
---

Header layout corrections (follow-up on user feedback):
1. Removed the duplicated fake macOS traffic-light dots. Window now uses titleBarStyle=Overlay (src-tauri/tauri.conf.json) so the OS draws the real traffic lights and the app header shares that row; header reserves left space (wp-traffic-space, 60px) and is a drag region (data-tauri-drag-region).
2. Header Left Group is now exactly 300px (= --wp-sidebar-width); the Top Action Group is right-aligned at that 300px edge (aligns above the sidebar's right border).
3. Meeting title is left-aligned immediately after the left group, offset by --wp-space-md (16px) to sit on the same vertical line as the 'Transcript' label below it (transcript header padding is also 16px). Removed the previous absolute-centered title.
Re-validation: tsc PASS, vitest 37/37 PASS, build PASS, prettier PASS on touched files.
Verification limitation: exact macOS traffic-light vertical placement in the 52px overlay bar can only be confirmed by launching the app (npm run tauri:dev); verified layout via updated preview artifact https://claude.ai/code/artifact/fd302060-0e46-4262-bd89-680c897718bf
