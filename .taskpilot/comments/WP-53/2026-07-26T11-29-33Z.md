---
schema_version: 1
created_at: '2026-07-26T11:29:33Z'
created_by: Aleksei_Platkovskii
---

Scope narrowed (user decision, 2026-07-26): the one-hop embedding-model fallback is split out to WP-57, which this item now blocks.

Removed from this item's DoD (3 criteria, all moved verbatim in substance to WP-57): the one-hop retry with the other model, the not-downloaded handling, and the distinct fallback warning strings. DoD 11 -> 8 criteria.

Design decisions 3, 4 and 5 in the description are marked MOVED TO WP-57 rather than deleted, so the record shows where they went. Decision 8 was reworded: this item must report the four child outcomes distinguishably (success / exit-by-signal / non-zero exit / timeout kill) and fails open on all failures, because WP-57's fallback keys off exactly that distinction - it must not collapse them into one error. Decision 6 (no label provenance) now points at WP-57, since nothing in this item alone changes which model runs.

Design constraint recorded for the implementer: WP-57's fallback attempt must be able to run under the same isolation, so the child interface must not be designed to permit only one attempt per run.

What this item still owns: subprocess isolation, heartbeat/inactivity supervision, the four-outcome contract, explicit dylib resolution, the regression test against the real reproducer, and holding threshold at 0.9.
