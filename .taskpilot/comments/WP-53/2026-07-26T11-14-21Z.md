---
schema_version: 1
created_at: '2026-07-26T11:14:21Z'
created_by: Aleksei_Platkovskii
---

Record refreshed from the 2026-07-26 triage and the user's Option D design decision. Item stays 'backlog'; this is record maintenance, not an implementation start.

Corrected: the previously recorded crash boundary ('>= 0.94 crashed, 0.93 and below did not') was measured with campplus only and is superseded. titanet-large - the catalog's recommended diarization model, and the one active in the user's settings when the app died - crashes at the shipped threshold of 0.9. The 'as-yet-untested recording could in principle hit a lower boundary' caveat this item was created with turned out to be real, and it was the model, not the recording, that moved the boundary.

Added: the deterministic reproducer and faulting stack; the measured evidence that threshold tuning cannot satisfy crash safety and separation quality simultaneously for titanet-large; the eight approved design decisions; the LC_RPATH hazard and the packaged-.app dylib question it inherits; and the finding that no macOS permission is required because the parent already holds decoded samples so the child never touches a TCC-protected path.

DoD rewritten from 3 investigate-and-choose criteria to 11 objective ones. The first is marked satisfied by triage rather than deleted, so the record still shows the criterion was met rather than dropped.

Dependency: WP-54 (done) already persists the transcript before diarization starts, so a crash now costs only the speaker labels.

OPEN QUESTION for the user, deliberately not decided here: whether the one-hop campplus fallback (DoD criteria 4-6) splits into its own sibling task. The fallback is a separate observable behavior that depends on isolation existing, so isolation could land and ship on its own. Recommendation is to split; no item created without approval.
