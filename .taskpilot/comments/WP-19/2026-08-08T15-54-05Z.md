---
schema_version: 1
created_at: '2026-08-08T15:54:05Z'
created_by: Aleksei_Platkovskii
---

DoD evidence: 277 Rust tests pass (cargo nextest, +3 new: running_transcription_flag matching, TranscriptionCancelGuard register/clear-on-drop, cross-run isolation on drop), clippy -D warnings clean, cargo build clean, cargo fmt --check clean; 222 frontend tests pass (vitest, +5 new: Stop wiring to cancelTranscription, Stop disabled until running and once diarizing starts, cancelled-run error surfaced with no document created and Transcribe re-enabled, cancel-request failure surfaced without disturbing the in-flight run), tsc --noEmit clean, eslint clean, prettier --check clean, vite production build succeeds. Implementation: AppError::Cancelled; transcribe() takes a shared abort flag wired to whisper-rs's set_abort_callback_safe, checked both before and after state.full(); decode_and_transcribe also checks it before spawning the decode; AppState.running_transcription (single slot, matches the existing one-run-at-a-time WhisperUsageGuard) is registered/cleared via TranscriptionCancelGuard (RAII, exit-path-safe); new cancel_transcription command flips the flag; new update_segment/update_notes-style ipc.cancelTranscription + wired Stop button (disabled outside the transcribing phase). docs/architecture.md updated with the Stop-only-covers-transcription-phase scope boundary. Native Tauri UI smoke not run in this session (no interactive display) — external/pre-existing limitation, not a found defect.
