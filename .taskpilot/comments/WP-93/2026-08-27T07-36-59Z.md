---
schema_version: 1
created_at: '2026-08-27T07:36:59Z'
created_by: Aleksei_Platkovskii
---

Completion evidence — route WP-93:implement-feature:1 (attempt 3, after one failed manual-UI gate and one code-review rework).

DoD-1 header middle slot, keyboard-focusable, accessible names: label + role="switch" toggle + target-language select render between the title group and the action cluster; verified live at 1000x760 and 1440x900 with all four header controls tabIndex 0 and names "Live Translation", "Live Translation target language", "Prettify transcript", "MFU panel".
DoD-2 two-column paired-row grid on, single-column restored off, one scroll container: covered by React tests and verified live.
DoD-3 backfill oldest-first, single in-flight call, persisted results reused without a model call: covered by the queue tests plus a regression test added at attempt 3 (see below).
DoD-4 same-language paragraphs never sent to the model and mirrored muted: covered.
DoD-5 failed row shows a retry control, the queue continues, retry re-runs only that paragraph, no dialog and no capture interruption: covered.
DoD-6 dropdown locked while on; switch disabled with a stated reason when no LLM model is ready or a prettified transcript is active or pending; Prettify disabled with a stated reason while translation is on: covered.
DoD-7 MFU aside keeps its fixed width, each column honors its minimum, no column collapses at the minimum supported window width: verified by real-layout measurement (see defects below).
DoD-8 local validation (test-runner attempt 3): lint, format:check, tsc, vitest 339/339 across 28 files, coverage 95.78% statements / 97.82% lines, build, clippy -D warnings, cargo fmt --check, cargo test 334 lib tests, cargo build - all pass. Audit not applicable (no dependency-table change); real-Metal transcription gate assessed and not applicable.

Scenarios 1-4 all pass.

Scope addition: WP-92 persisted translations but exposed no way to read them back, so DoD-3's reuse requirement was unimplementable. This item added the read-only command list_streaming_translations(session_id, target_language) returning {paragraph_key, source_text, translated_text}, its ipc.ts wrapper listStreamingTranslations, and Rust tests for unsupported language, unknown session, ordering and per-session/per-language scoping.

Defects found by the gates and fixed:
1. Manual UI gate, attempt 1 - Fail. At the app's configured minimum window width (1000px) with the sidebar and MFU aside open the transcript panel is 380px, but a translation row required 441px, clipping the translated column mid-word. Fixed by deriving the per-column minimum from that narrowest layout; re-measured at columns 190/190 with zero overflow.
2. Manual UI gate, attempt 1 - Fail. The three-slot header needed 534px in the same 380px panel and, with overflow visible, the action cluster rendered outside the panel over the MFU aside. Fixed with a container query on a Streaming-only panel modifier that hides three non-essential text labels below a narrow-panel threshold; re-measured at 1000x760 as clientWidth 380 = scrollWidth 380, rightmost control right edge 664 inside the panel right edge 680, all controls focusable with unchanged accessible names, and at 1440x900 with all labels visible and zero overflow.
3. Code review, attempt 2 - Needs revision, one Major: persistedReady was reset only inside the persisted-fetch effect, so a second activation within one session reconciled against a stale open gate and fired a live model call for paragraphs that already had persisted rows, whose result then overwrote the cached text. Fixed test-first; the regression test failed Red with "expected vi.fn() to not be called at all, but actually been called 1 times" before the synchronous gate reset in handleToggleTranslation. Re-review confirmed handleToggleTranslation is the only path setting translationEnabled true and every session path forces it false, so the single gate closes all routes.
4. Code review, attempt 2 - Minor: the container query had been attached to the shared .wp-transcript-panel class, silently hiding Meeting's segment-count label. Rescoped to a .wp-transcript-panel--streaming modifier; Meeting re-verified at 1000x760 with its meta label back to display: block and zero header overflow.
Two further Minors (CSS comments narrating measurements, missing test-technique tags) were also fixed. Code review attempt 3: Approved with minor notes; both remaining notes are Info-level optional follow-ups.

Documentation: documentation updated - docs/architecture.md corrected its stale "core and persistence only, no UI yet" heading, documented the read command and the front-end queue behavior, and gained IPC-contract rows for both translation commands; docs/design.md documents the three-slot Streaming header, the locked dropdown, the mutual exclusion with Prettify, the paired-row view with its five row states, and the narrow-width label collapse. The ADR and feature-folder records remain with WP-95.
Quality gate: pass.

External verification limitation (no implementation defect): translation row states, queue ordering, persisted reuse, the locked dropdown and the Prettify exclusion could not be exercised in the native Tauri window - verification ran in a browser pane where invoke is unavailable and no LLM model is detectable. All are covered by the tests in StreamingView.translation.test.tsx.
