---
schema_version: 1
created_at: '2026-08-10T11:49:33Z'
created_by: Aleksei_Platkovskii
---

Completion evidence (WP-84:fix-bug:1, task-quality gate: pass).

Root cause: whisper-rs 0.16.0 set_abort_callback_safe/set_progress_callback_safe box the closure as Box<dyn FnMut> (fat pointer) but their FFI trampoline reinterprets user_data as the concrete closure type F - UB; the abort-flag read dereferenced heap garbage, randomly tripping whisper.cpp's abort check -> `failed to encode` (-6), and in the user's latest build a SIGSEGV inside the downcast trampoline.

Fix: src-tauri/src/transcribe.rs installs cancel/progress through whisper-rs's raw setters (set_abort_callback(+_user_data), set_progress_callback(+_user_data)) with our own sound trampolines whose user_data points at the live AtomicBool / the concrete progress closure for the duration of the synchronous state.full call. Decode params, cancellation UX, IPC events, persistence, whisper-rs version unchanged; unsafe confined to trampoline wiring.

DoD evidence:
1. abort_trampoline_returns_the_exact_flag_value (unit test) - Green.
2. progress_trampoline_forwards_the_exact_progress_value (unit test) - Green.
3. Call-site grep for set_abort_callback_safe(/set_progress_callback_safe( under src-tauri/ - zero hits.
4. Ignored stress test false_cancel_flag_never_aborts_the_decode (real model, CPU backend): 5/5 consecutive decodes Green post-rework; pre-fix the same path failed 2/3 CPU runs.
5. set_cancel_flag_cancels_the_run: Stop still aborts with AppError::Cancelled - Green.
6. Validation: Skill validate PASS; test-runner Pass (cargo test, vitest 259/259, eslint, tsc, coverage 95.74%); code-reviewer Approved with minor notes, both reworked and re-verified.

Docs: docs/architecture.md (callback-UB note + WP-82 Metal known limitation), docs/development.md (WHISPERPILOT_TEST_AUDIO env var); sdd-index-sync completed, no row changes.

External verification limitation (non-blocking): automated stress deliberately used the CPU backend; the separate upstream ggml Metal encoder fault on M5 Pro (whisper.cpp 1.8.3, reproduces on main with no callbacks) is out of scope and documented as a known limitation.
