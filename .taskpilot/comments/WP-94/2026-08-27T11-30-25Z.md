---
schema_version: 1
created_at: '2026-08-27T11:30:25Z'
created_by: Aleksei_Platkovskii
---

Completion evidence — route WP-94:implement-feature:1 (attempt 2, after a placeholder-semantics rework and three code-review cleanups).

DoD-1 paired original + translation blocks per paragraph in screen order for the selected target language: renderStreamingPaired in src/export.ts, covered by unit and component tests.
DoD-2 output byte-identical when Live Translation is off or no translation exists: the off branch calls the same pre-existing plainTranscript(windows) rather than a re-derived equivalent, so it cannot drift; all 20 pre-existing export tests pass unmodified (git diff --numstat src/export.test.ts = 256 insertions, 0 deletions).
DoD-3 explicit placeholders keep paragraph-count parity: the renderer is a 1:1 map with no filter path; parity proven by a combinatorial test plus an empty-session test.
DoD-4 Copy and Export share one renderer: a single exportText value feeds both CopyButton and handleExport, asserted identical by a dedicated test.
DoD-5 local validation (test-runner attempt 2): lint, format:check, tsc, vitest 364/364 across 29 files, coverage 95.83% statements / 97.85% lines, build - all pass, re-confirmed after the cleanup edits. No Rust file changed, so cargo checks, audit and the real-Metal transcription gate are all not applicable. Manual UI verification not applicable - no visual or interaction surface changed.

Scenarios 1-3 pass, plus both named edge cases: a stale stored translation exports the original with the not-translated placeholder (exact source-text comparison), and a session translated earlier into a different target language exports only for the selected one.

Defect corrected during this item: the first implementation used a single placeholder for all five causes, including mirrored paragraphs. A mirrored paragraph is already entirely in the target language, so no model call is made by design and the screen shows its own text; stamping "[Not translated]" over it would have marked every English paragraph of a mixed session as missing, contradicting the confirmed decision that copy and export reproduce what the screen shows. Corrected so a mirrored paragraph exports its own text, a failed translation reports "[Translation failed]" (distinct wording, because a failure needs a manual retry rather than more waiting), and absent, pending or stale paragraphs report "[Not translated]". A stale mirrored entry falls back to the not-translated placeholder rather than exporting text that no longer matches the paragraph. The tests that had asserted the old behavior encoded the defect and were corrected as part of the fix.

Code review: Approved with minor notes. All three notes closed in this pass - the rejected-alternative narration on the failed-placeholder constant was trimmed to the bare constraint, the duplicated target-language name map was collapsed to one exported definition in export.ts that StreamingView imports (so adding a third language is a one-place edit), and two test comments citing git history as their record were trimmed.

Documentation: skipped - no authoritative documentation fact changed. docs/design.md already documents Streaming copy/export and the WP-93 entry states the user-visible contract that export reproduces what the screen shows; per-state placeholder strings are a rendering detail. Feature-level documentation belongs to WP-95.
Quality gate: pass.
