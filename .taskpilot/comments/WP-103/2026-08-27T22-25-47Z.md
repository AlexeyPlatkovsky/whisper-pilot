---
schema_version: 1
created_at: '2026-08-27T22:25:47Z'
created_by: Aleksei_Platkovskii
---

Completion evidence (task-quality gate: pass).

Replaced paragraph-batch Live Translation with rolling per-window translation (ADR-016, superseding ADR-015's "Unit" decision): translated text now appears roughly 7-14s behind speech instead of up to 28s (post-WP-100) / 42s (pre-WP-100).

DoD 1-11 all pass:
1. Per-window trigger at windows.length >= 2; bootstrap pair (window 0 no context, window 1 context=window 0) fire together; every later window translates alone with up-to-2-window rolling context, skipping unavailable/failed predecessors.
2. isParagraphClosed deleted (dead code, sole caller removed); groupWindowsIntoParagraphs unchanged, display-only now.
3. streaming_translations.paragraph_key renamed to window_index via checked, idempotent ALTER TABLE ... RENAME COLUMN migration.
4. translate_streaming_paragraph renamed translate_streaming_window (param paragraph_key -> window_index); llm.rs signatures byte-identical, comments only.
5. Persisted-row reuse matches by window_index + that window's own current text.
6. Shared per-window display helper (streamingText.ts) used by both the on-screen renderer and export.ts - partial paragraphs show real text for finished windows + a placeholder only for the unfinished tail.
7. Retry stays paragraph-scoped, re-enqueuing only failed windows within it.
8. Per-window mirroring - a paragraph mixing already-translated and needs-translation windows now renders both correctly (fixes WP-93's old all-or-nothing check).
9. Backfill still walks windows oldest-first through the same single-flight queue.
10. WP-101 translation-enabled persistence and WP-99 target-language non-persistence unchanged (grep-confirmed, existing suites pass).
11. Local validation: cargo test --lib 356/356 pass; cargo clippy --all-targets -D warnings clean; cargo fmt --check clean; cargo build clean; vitest run --coverage 380/380 pass (95.77% stmts / 97.91% lines, at/above the 95% gate); tsc --noEmit clean; eslint clean; prettier --check clean; vite build succeeds.

TDD: Red evidence captured before each half's production edits - backend: 3 new tests failed with compile errors (E0560, missing window_index field) against the pre-rename structs; frontend: 39 of 66 new/rewritten tests failed meaningfully (wrong values, zero calls, shape mismatches) against pre-rewrite code, confirmed via git-stash-based isolation.

Code review: Approved on both halves (backend: Agent: code-reviewer task a8dc4ed528a8d8341; frontend: task a2aecad76634990de). Three Minor comment-length findings (migration doc comment; runTranslationQueue/reconcile-effect/precedingWindowsContext comments) fixed and re-validated clean, full suite still green after trims.

Documentation: docs/architecture.md (Live Translation section renamed and rewritten, IPC table row updated, Streaming Persistence paragraph updated for the window_index rename), docs/design.md (paired-row partial-rendering states rewritten for per-window fill-in), docs/glossary.md (split the stale "Paragraph (translation unit)" entry into separate Window/translation-unit and Paragraph/display-unit entries), docs/decisions/ADR-016 (new - records the reversal of ADR-015's paragraph-unit decision, its context, consequences including the accepted one-time retranslation and fragment-quality tradeoffs, and alternatives considered), docs/decisions/ADR-015 (marked partially superseded at the specific "Unit" decision and its "Translate at window granularity" rejected alternative - engine reuse, concurrency, target languages, and persistence-reuse concept all still stand), docs/INDEX.md (via sdd-index-sync - ADR-016 row added, ADR-015 status updated).
