---
schema_version: 1
created_at: '2026-08-27T02:28:52Z'
created_by: Aleksei_Platkovskii
---

Completion evidence — route WP-96:implement-feature:1 (attempt 2 after code-review rework).

DoD-1 renders at the right end of both transcript headers, keyboard-focusable, aria-checked exposed: ToggleSwitch.test.tsx (role/name/checked/keyboard) + DOM-order assertions in App.mfuToggle.test.tsx and StreamingView.mfuToggle.test.tsx; live check confirmed role="switch" named "MFU panel", cluster order and 28x16 geometry.
DoD-2 off hides aside.wp-mfu and the transcript takes the full workspace width; on restores: vitest off/on tests on both screens; live measurement 980px (= workspace) hidden, 660px shown.
DoD-3 per-screen persistence via mfu_panel_meeting / mfu_panel_streaming, restore on launch, read/write failure falls back to ON: 8 Rust settings tests (defaults, per-key persistence, independence, invalid-value rejection leaving the store unchanged, corrupt-file fallback, pre-WP-96 store file defaulting to true) plus front-end launch-restore and both failure-fallback tests.
DoD-4 Craft MFU while hidden reveals the panel and leaves the switch on without altering generation: Craft-reveal test on both screens asserting the generate call fires exactly once.
DoD-5 Craft MFU, Prettify, Diarize, Transcribe, Start/Stop keep their enabled/disabled behavior: assertions now cover all five controls; no disabled expression references mfuPanelVisible.
DoD-6 local validation: test-runner Pass on lint, format:check, tsc, vitest (318/318 in 27 files), coverage (95.93% statements / 97.7% lines, threshold 95%), build, clippy -D warnings, cargo fmt --check, cargo test (293 lib tests), cargo build. Real-Metal transcription gate not applicable - no transcription, decode, diarization or model code touched.

Scenarios 1-3 all pass (see DoD-2, DoD-4, DoD-3 rows respectively).
Code review: Approved with minor notes; both notes (pre-upgrade settings-file test, Transcribe/Start/Stop invariance assertions) closed in attempt 2.
Documentation: documentation updated - docs/architecture.md settings-key enumeration and docs/design.md transcript-header + MFU-section hidden state; sdd-index-sync completed with docs/INDEX.md unchanged.
Quality gate: pass.

External verification limitation (no implementation defect): keyboard activation, settings persistence across a real restart, per-screen independence across restart and the Craft-MFU reveal could not be exercised in the native Tauri window - verification ran in a Chromium browser pane where invoke is unavailable and synthetic key events do not activate any native button (control-tested against a pre-existing button). All four are covered by the automated tests listed above.
