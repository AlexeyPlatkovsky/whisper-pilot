---
schema_version: 1
created_at: '2026-08-27T18:20:57Z'
created_by: Aleksei_Platkovskii
---

Roll-up: all direct children (WP-92, WP-93, WP-94, WP-95, WP-100, WP-101) are done.

This feature was previously closed after WP-95 with its DoD's real-Metal-run and full-validation evidence already recorded. It was reopened to track two follow-on fixes reported from that same live run:

- WP-100: reduced worst-case translation latency from 42s to well under half that (paragraph window cap 6->4, plus prior-paragraph context for translation consistency). Does not touch transcribe.rs/audio.rs/Whisper model loading, so the real-Metal transcription regression gate is not applicable; confirmed by diff scope.
- WP-101: fixed two session-lifecycle bugs (starting/resuming the currently-open session no longer silently turns Live Translation back off; the enabled/disabled state is now persisted per session in streaming_sessions and survives reopening a session and an app restart). Also not applicable to the real-Metal gate for the same reason.

Both children passed their own task-quality DoD gates, code review, and full local validation (cargo test, clippy -D warnings, cargo fmt --check, vitest with coverage at/above the 95% gate, tsc, eslint, prettier --check, cargo build, vite build) independently — see their own completion comments for the itemized evidence.

WP-91's own DoD items (child completeness, translated-session parity with real Metal audio, Prettify mutual exclusion, target-language lock, persisted-translation reopen without re-running the LLM, full local validation) were satisfied and recorded at the original close; nothing in WP-100/WP-101 reopens or contradicts that evidence.
