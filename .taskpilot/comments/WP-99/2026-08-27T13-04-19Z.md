---
schema_version: 1
created_at: '2026-08-27T13:04:19Z'
created_by: Aleksei_Platkovskii
---

Completion evidence — route WP-99:implement-feature:1.

Scenario: switching Live Translation on without touching the dropdown now yields Russian, with the split view's target column headed РУССКИЙ. Covered by the select initial-value test and the column-header test.
Scope boundary held: only the targetLanguage initial state changed in production. Persistence (still none), the supported language set, the dropdown lock, the queue and the translation backend are untouched.
Edge case: the same-language mirror path is now taken by Russian paragraphs rather than English ones; both the unit and paired-export mirror tests were re-pointed so they still prove the skip.

Implemented test-first: 21 tests across the two Streaming test files were failed Red against the old English default before the one-line production change, then went green.

Test-fixture handling reviewed specifically because flipping a default can silently weaken a suite: makeWindows' default fixture language moved from ru to en so fixtures still differ from the target and exercise real translation instead of the mirror path; the cross-language leakage test had its roles swapped so it now persists an en row against a ru default; tests that pass a language literal explicitly were left untouched.

Validation: lint, format:check, tsc, vitest 364/364 across 29 files, coverage 95.83 percent statements / 97.85 percent lines, build - all pass. No Rust change needed.
Code review: Approved, no findings. The reviewer confirmed no test now asserts a weaker or vacuous condition, that every remaining en literal is intentional, that no default assertion was missed, and that the design.md and ADR-015 edits are accurate, minimal and inside their ownership boundaries.
Documentation: docs/design.md and ADR-015 record the new default and that English to Russian is the primary direction; the not-persisted fact is preserved.
Quality gate: pass.
