---
schema_version: 1
created_at: '2026-08-27T18:19:57Z'
created_by: Aleksei_Platkovskii
---

Completion evidence (task-quality gate: pass).

DoD 1-9 all pass:
1. startSession resets translation state only on resumeId===null or session-identity change; Bug-1 repro test passes.
2. translation_enabled column added, migration defaults existing rows to false, exposed on record/summary/DTOs; migration + idempotent-reopen tests pass.
3. Store method set_translation_enabled mirrors rename_session's shape; persistence + unknown-id-error tests pass.
4. Tauri command set_streaming_translation_enabled registered in lib.rs; ipc.ts wrapper added.
5. handleOpen restores translationEnabled from the opened session's persisted value; test passes.
6. Toggle persists best-effort (WP-96 pattern); failure leaves switch as user set it; test passes.
7. Opening a different session resets to its own persisted value; brand-new session starts off; delete has no orphan (same-row column, existing cascade); tests pass.
8. WP-99 target-language persistence and WP-96 MFU-toggle pattern unchanged (grep-confirmed, existing suites unaffected).
9. Local validation: cargo test --lib 353/353 pass; cargo clippy --all-targets -D warnings clean; cargo fmt --check clean; vitest run --coverage 383/383 pass (95.78% stmts / 97.89% lines, at/above the 95% gate); tsc --noEmit clean; eslint clean; prettier --check clean; vite build succeeds.

Scenarios 1-3 (same-session resume keeps translation on; reopened/restarted session restores switch; different session reflects its own state) all pass via the tests above.

Code review (Agent: code-reviewer, task ab7dadab0151e302f): Approved with minor notes. Verified migration idempotency, startSession reset-boundary logic, best-effort persistence shape, DTO completeness, and scope discipline (WP-99/WP-96 untouched) as correct. Two minor findings fixed post-review: (1) trimmed a change-hygiene-violating decision-narrative comment in StreamingView.tsx's startSession; (2) made ipc.ts's translation_enabled field non-optional (matching the Rust DTOs, which always serialize it) and updated the fixture literals across the four Streaming test files accordingly. Full suite re-validated green after both fixes.

Documentation: docs/architecture.md updated — the Paragraph Translation lifecycle section (Streaming, WP-93/WP-100 area) gained a paragraph describing the new persisted switch state and the same-session-resume exception; the IPC contract table gained the set_streaming_translation_enabled row. docs/design.md and docs/INDEX.md required no change (no new UX flow, no generated-index region affected).

TDD: Red evidence captured before production edits (8 new front-end test cases, 12 new Rust test functions, matching the reported vitest 375->383 and cargo-test 341->353 deltas) — independently corroborated by the code-reviewer by counting the new test blocks in the diff.
