---
schema_version: 1
created_at: '2026-08-27T21:30:54Z'
created_by: Aleksei_Platkovskii
---

Completion evidence (task-quality gate: pass).

Root cause: src/StreamingView.tsx's handleOpen, startSession's different-session branch, handleCreateNew, and confirmDelete all cleared persistedTranslationsRef.current without resetting the persistedReady state flag, letting a stale persistedReady=true from a prior session race the reconcile effect ahead of the reopened session's own async persisted-translations fetch. Fixed by pairing every persistedTranslationsRef clear with a synchronous setPersistedReady(false), the same invariant WP-93 already established for handleToggleTranslation.

DoD 1-3 pass:
1. All four sites fixed; regression test "reopening a session with an already-persisted paragraph reuses it without a new model call, even after visiting another session in between" reproduces the exact reported sequence (stop A with translation on -> switch to B -> return to A) and passes.
2. No-persisted-row live-translation happy path unaffected (unchanged code path, pre-existing test coverage).
3. Local validation: cargo test --lib 353/353 pass; cargo clippy --all-targets -D warnings clean; cargo fmt --check clean; cargo build clean; vitest run --coverage 384/384 pass (95.79% stmts / 97.9% lines, at/above the 95% gate); tsc --noEmit clean; eslint clean; prettier --check clean; vite build succeeds.

TDD: Red evidence captured before the fix - the regression test failed with `translateStreamingParagraph` called once for session 1's already-persisted paragraph 0 (AssertionError: expected "vi.fn()" to not be called at all, but actually been called 1 times), exactly reproducing the reported bug.

Code review (Agent: code-reviewer): Approved, second pass. First pass blocked only on missing literal validation/Red-evidence artifacts (the agent has no Bash access); resupplied as literal text and independently re-verified against the actual files. Two Minor comment-length findings (change-hygiene.md section 5, both trimmed to 3 lines) and one Info DoD-wording gap (updated to name all four fix sites, not three) fixed and confirmed.

Documentation: no update needed - pure implementation-defect fix; docs/architecture.md's existing description of persisted-translation reuse already describes the intended behavior correctly, the bug was in achieving it, not in what's documented.
