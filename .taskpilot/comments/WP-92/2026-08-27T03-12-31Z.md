---
schema_version: 1
created_at: '2026-08-27T03:12:31Z'
created_by: Aleksei_Platkovskii
---

Completion evidence — route WP-92:implement-feature:1 (attempt 2 after code-review rework).

DoD-1 prompt + rejection rules: build_translate_prompt branches on the target language; validate_translation_candidate rejects empty/whitespace, disproportionate length, and still-in-source-script results. 8 llm::tests cover prompt construction and every rejection rule (BVA/EP tagged).
DoD-2 typed errors with no row written for no active model, unknown session, unsupported target language, empty source, and rejected candidate: ensure_translation_request_is_valid_rejects_* (x3), resolve_llm_model_path_errors_when_no_model_is_selected, translate_and_store_writes_no_row_when_translation_is_rejected. Error-path ordering verified in review: validate -> resolve model -> acquire guard -> translate -> persist.
DoD-3 streaming_translations table with PK (session_id, paragraph_key, target_language), ON CONFLICT DO UPDATE upsert, ON DELETE CASCADE: streaming_store tests incl. overwrite-not-duplicate, cascade-on-session-delete, and nonexistent-session error. PRAGMA foreign_keys = ON runs before schema creation, so the cascade test passes for the right reason.
DoD-4 migration preserves prior data: opening_a_pre_migration_database_preserves_existing_data_and_adds_translations_table, using a hand-built pre-migration SQLite fixture that includes the legacy streaming_notes -> streaming_mfu rename path.
DoD-5 read by session + target language and stale detection: list_translations plus StreamingTranslation::is_stale, covered by independence-by-session, independence-by-language, stale and not-stale tests.
DoD-6 src/ipc.ts exposes translateStreamingParagraph and StreamingTranslationTargetLanguage; signature matches the Rust command.
DoD-7 local validation (test-runner attempt 2): lint, format:check, tsc, vitest 318/318, build, clippy -D warnings, cargo fmt --check, cargo test 327 lib tests, cargo build - all pass. Re-verified after the final comment trim: fmt clean, clippy clean, 327 lib tests pass. Coverage excluded (ipc.ts change is a thin typed invoke wrapper with no logic); audit not applicable (no dependency-table change); manual UI verification not applicable (no UI surface); real-Metal transcription gate assessed and not applicable (no transcription, decode, Whisper-context or Metal build-config code touched).

Scenarios 1-3 all pass.
Single-flight: AppState::translation_busy (AtomicBool) + llm::TranslationUsageGuard, a non-blocking compare-exchange RAII guard released on every return path including a JoinError from a panicked blocking task. Deliberately independent of whisper_busy so translation can never block or be blocked by the streaming decode loop. Contention returns AppError::TranslationBusy, a distinct UI-retryable error.
Code review: attempt 1 Needs revision - one Major (chars/4 token estimate was Latin-calibrated; a 54,000-char Cyrillic paragraph estimated ~13,725 tokens against a 16,384 budget while really costing ~28,225) and two Minor. Fixed test-first with confirmed Red, then estimated_token_count was made script-aware via the existing dominant_script machinery. Attempt 2: Approved with minor notes; the final note (comment trim) applied and re-verified.
Documentation: documentation updated - docs/architecture.md gained the streaming_translations table description and a Paragraph Translation section, the IPC contract table gained translate_streaming_paragraph plus four rows for pre-existing commands that had drifted out of it, and the inaccurate "Craft and Prettify are mutually exclusive in flight" claim was corrected to state that the exclusion is front-end only. sdd-index-sync completed, docs/INDEX.md unchanged.
Quality gate: pass.
