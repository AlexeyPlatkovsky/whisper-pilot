# Design Book

<!-- WhisperPilot-specific extension document. Owns design-system tokens, theme mappings,
     reusable patterns, and component-style rules. Keep product flows, screens, and states
     in design.md; link there instead of duplicating them. Register this extension in INDEX.md. -->

## Tokens And Themes

<!-- Semantic color, spacing, typography, radius, motion, and icon tokens plus light/dark
     mappings. Point to the token source when one exists. -->

## Reusable Patterns

<!-- Shared component patterns, their allowed variants, and accessibility constraints. -->

## Component Styles

<!-- Reusable component styling rules. Keep feature-specific layout in feature docs or code. -->

## Change Control

<!-- How a new token or reusable pattern is approved and synchronized with its source. -->
